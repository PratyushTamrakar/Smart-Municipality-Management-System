/**
 * Run THIS class (not Main) if you hit
 * "Error: JavaFX runtime components are missing" — it avoids the module-path
 * detection issue by not being an Application subclass itself.
 */
public class MainLauncher {

    public static void main(String[] args) {
        Main.main(args);
    }
}